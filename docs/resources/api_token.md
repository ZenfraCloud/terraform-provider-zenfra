---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "zenfra_api_token Resource - zenfra"
subcategory: ""
description: |-
  Manages a Zenfra API token. The token value is only available at creation time.
---

# zenfra_api_token (Resource)

Manages a Zenfra API token. The token value is only available at creation time.

## Example Usage

```terraform
resource "zenfra_api_token" "ci" {
  name            = "CI Pipeline Token"
  description     = "Token for CI/CD automation"
  role            = "write"
  expires_in_days = 90
}

# The token value is only available at creation time.
output "ci_token" {
  value     = zenfra_api_token.ci.token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the API token.
- `role` (String) The role for this API token (read, write, admin).

### Optional

- `description` (String) Description of the API token.
- `expires_in_days` (Number) Number of days until the token expires. 0 means no expiration. Defaults to 90 days if not specified.

### Read-Only

- `active` (Boolean) Whether the token is currently active.
- `created_at` (String) Timestamp when the token was created.
- `expires_at` (String) Timestamp when the token expires.
- `id` (String) The unique identifier of the token.
- `last_used_at` (String) Timestamp when the token was last used.
- `token` (String, Sensitive) The API token value. Only available after creation.
- `token_prefix` (String) The first 8 characters of the token for identification.
- `usage_count` (Number) Number of times the token has been used.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import zenfra_api_token.ci $TOKEN_ID
```
